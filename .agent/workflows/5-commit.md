---
description: Ship phase - Commit changes using Conventional Commits
---

1. Run `./dev.sh exec main flake8 .` to check linting.
2. Stage changes: `git add .`.
3. Commit with prefix `[#issue]` followed by conventional commit message (e.g., `[#34] feat(ai-stack): add new AI components`).
4. Push to remote.
